# ✅ Final Lab - Student Management System (SMS)

## 🎯 วัตถุประสงค์
- ฝึกออกแบบและเขียนโปรแกรมเชิงวัตถุ (OOP) ใน Python  
- สร้างระบบจัดการข้อมูลนักศึกษา (เพิ่ม, ลบ, แก้ไข, แสดงผล)  
- เข้าใจหลักการ **Inheritance, Polymorphism, Encapsulation, Abstraction**

---

## 🏗️ คลาสหลักที่ต้องทำ

### 1. `Student` (Abstract Class)
- **Attributes**
  - `name`
  - `student_id`
  - `major`
  - `gpa`
- **Methods**
  - `calculate_scholarship()` → abstract
  - `display_info()`

---

### 2. `Undergraduate` (นักศึกษาปริญญาตรี)
- **Inheritance**: สืบทอดจาก `Student`
- **Attributes**
  - `year` (ชั้นปี 1–4)
- **Methods**
  - Override `calculate_scholarship()`  
    ➝ ทุน = `(GPA × 1000) + (200 × year)`

---

### 3. `Graduate` (นักศึกษาปริญญาโท/เอก)
- **Inheritance**: สืบทอดจาก `Student`
- **Attributes**
  - `research_topic`
- **Methods**
  - Override `calculate_scholarship()`  
    ➝ ทุน = `(GPA × 2000) + (500 ถ้ามี research_topic)`

---

### 4. `Registrar` (งานทะเบียน)
- **Attributes**
  - `students` (list เก็บ object ของนักศึกษา)
- **Methods**
  - `add_student(student)`
  - `remove_student(student_id)`
  - `find_student(student_id)`
  - `display_all_students()`

---

## 📝 สิ่งที่ต้องทำ
- [ ] ออกแบบและสร้างคลาส `Student`, `Undergraduate`, `Graduate`, `Registrar`
- [ ] ใช้ `__init__` กำหนดค่าเริ่มต้น
- [ ] ใช้ `abstractmethod` ใน `Student`
- [ ] ใช้ **Encapsulation** (เช่น ซ่อน `gpa`)
- [ ] ใช้ **Polymorphism** กับ `calculate_scholarship()`

---

## 🧪 การทดสอบโปรแกรม
- [ ] สร้างออบเจกต์ `Registrar`
- [ ] เพิ่มนักศึกษา (ทั้ง `Undergraduate` และ `Graduate`)
- [ ] ทดลอง:
  - [ ] ลบข้อมูล
  - [ ] ค้นหาข้อมูล
  - [ ] แสดงข้อมูลนักศึกษา
- [ ] แสดงทุนของนักศึกษาแต่ละคน

---
